import { Typography,Box } from "@mui/material"

const WeatherBlocks = () =>{
    return(
        <Box>
            <Typography variant="h5">Humidity</Typography>
        </Box>
    )
}

export {WeatherBlocks};